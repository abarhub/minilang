# architecture


Les exemples de code sont dans le répertoire examples/
La documentation est dans le répertoire docs/
Les sources sont dans le répertoire src/
Les tests sont dans le répertoire tests/
La librairie standard est dans le répertoire lib/std/

Un projet minilang peut être configuré par un fichier optionnel minilang.toml
(voir configuration.md) : point d'entrée, profil DI, niveau de log.



