// ── Bibliothèque standard – Map<K,V> ─────────────────────────────────────────
//  Interface pour les tables associatives.

interface Map<K, V> {
    void put(K key, V value);
    V get(K key);
    bool containsKey(K key);
    int size();
    bool isEmpty();
    bool remove(K key);
    void clear();
    string toString();
}
