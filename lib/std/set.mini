// ── Bibliothèque standard – Set<T> ───────────────────────────────────────────
//  Interface pour les ensembles sans doublons.

interface Set<T> {
    bool add(T element);
    bool contains(T element);
    int size();
    bool isEmpty();
    bool remove(T element);
    void clear();
    string toString();
}
